daemons:
  openvpn:
    run:
      cmd: |
        sudo openvpn --config $OPENVPN_CONFIG_FILE
  jupyter-notebook:
    run:
      cmd: |
        bash -c "/home/owner/dev/apps/start_jupyter_notebook.sh"
